{% extends 'base.html.twig' %}

{% block title %}Hello FrontController!{% endblock %}

{% block body %}
<div class="example-wrapper mainbg">
    <div class="card text-center my-5" id="cardtestorange" style="width: 50rem;">
        <div class="card-header">
            <h2>{{project.title}}</h2>
            <h5 class="card-title">Create the : {{project.createdate|date('d/m/Y')}}</h5>
        </div>
        <div class="card-body">
            <p class="card-text">{{project.description}}</p>
        </div>
        <div class="card-footer text-muted">
            Deadline : {{project.deadline|date('d/m/Y')}}
        </div>
    </div>
</div>
  
    {# <h2>Answers</h2> #}
{# {% if subject.answers %} #}
    {# {% for answer in subject.answers %} #}
    {# <p>{{subject.published|date('d/m/Y')}} at {{subject.published|date('H:i')}} by : {{answer.user.firstname ~" "~ answer.user.lastname}}</p> #}
    {# <div id="rules"> #}
        {# <p>{{answer.content}}</p> #}
        {# <a href="{{path('newcomments', {answerId: answer.id})}}" class="btn btn-sm text-dark float-end">Comment</a> #}
    {# </div> #}
{# {% for comment in answer.comments %} #}
    {# <p class="text-white text-italic">{{comment.content}} by {{comment.user.firstname ~" "~ comment.user.lastname}}</p> #}
    {# {% endfor %} #}
{# {% endfor %} #}
{# {% else %} #}
    {# <div class="alert alert-secondary"> No answer yet </div> #}
{# {% endif %} #}
    {# <hr> #}
    {# <h2>Post an Answer</h2> #}
    {# {{form(form)}} #}
{% endblock %}
