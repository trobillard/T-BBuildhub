{% extends 'base.html.twig' %}

{% block title %}TㅆB Build Project Home{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper mainbg">
  <button type="button" class="btn" data-bs-toggle="modal" data-bs-target="#exampleModal">
    New Project
  </button>

   {% for label, messages in app.flashes %}
      {% for message in messages %}
      <div class="alert alert-light my-5 {{ label }}">
        {{ message }}
      </div>
      {% endfor %}
  {% endfor %}
  {# Afficher tous les messages flash en session pour l utilisateur connectes #}


  <div class="row">
    {% for project in projects %}
    <div class="card text-center my-3 me-4 col-sm-12 col-md-6 col-lg-4 p-4" id="cardtestorange" style="width: 15rem;">
    <div class="card-header">
      <h4 class="card-title">{{project.title}}</h4>
      <h5 class="card-title">Created the : {{project.createdate|date('d/m/Y')}}</h5>
    </div>
    <div class="card-body">
    <p class="card-text">{{project.description}}</p>
    <a href="{{path('task_index', {id:project.id})}}" class="btn1">Go to the Task</a>
  </div>
  <div class="card-footer text-muted">
    Deadline : {{project.deadline|date('d/m/Y')}}</br>
  </div>
  <div>
    <a href="{{ path('single', {'id': project.id}) }}" class="btn1">Show</a>
   </div>
</div>

    {% endfor %}
</div>
   <div class="modal fade " id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">New Project</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
     {{form(form)}}

      </div>
    </div>
  </div>
</div>
</div>

{% endblock %}
